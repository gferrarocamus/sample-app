<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% else %>
  <p class="alert alert-warning">Couldn't load feed :(</p>
<% end %>